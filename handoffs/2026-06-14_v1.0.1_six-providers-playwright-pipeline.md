# Handoff — code-smart — 2026-06-14

**Version:** v1.0.1
**HEAD:** `084020f`
**Production:** not yet deployed — wrangler.jsonc configured, Cloudflare Pages deploy is next step

---

## Session Outcomes

| Item | Status |
|------|--------|
| Add 6 new provider JSON files (Kimi, Qwen, Copilot Xcode, OpenCode, Mimo, MiniMax) | ✅ Shipped |
| Register all 6 in data-loader.ts | ✅ Shipped |
| Playwright fetch + diff pipeline (`scripts/fetch-provider.ts`) | ✅ Shipped |
| GitHub Actions daily-check workflow | ✅ Shipped |
| GitHub repo created + all commits pushed | ✅ Shipped |
| Gemini Council review on plan (3 REVISE findings, all fixed) | ✅ Done |

---

## What Shipped
- `src/data/providers/kimi.json` — Kimi (Moonshot AI): 3 plans, CNY→USD assumed, moonshot-v1-128k + Kimi k1.5 models
- `src/data/providers/qwen.json` — Qwen/Alibaba: API-only, 2 plans, Qwen2.5-Coder-32B + QwQ-32B
- `src/data/providers/copilot-xcode.json` — GitHub Copilot for Xcode: 3 plans, mirrors Copilot pricing, Xcode-specific features
- `src/data/providers/opencode.json` — OpenCode: 1 plan ($0 BYOK, `confidence: "observed"`), MIT-licensed CLI agent
- `src/data/providers/mimo.json` — Mimo: 2 plans, education platform (explicitly flagged as non-professional)
- `src/data/providers/minimax.json` — MiniMax: API-only, 2 plans, 1M token context window
- `src/lib/data-loader.ts` — 6 new imports + PROVIDER_FILES entries (11 total providers, 30 plans)
- `scripts/fetch-provider.ts` — Playwright screenshot + USD price regex diff; exit 1 on MISMATCH/NOT_FOUND; local-only (WAF blocks CI runners)
- `.github/workflows/daily-check.yml` — 9am UTC daily: validate → stale-check → build; `permissions: issues: write`; deduped GH issue on stale data
- `package.json` — playwright devDep + "fetch" script

## Validation Results
- `npm run validate`: 11/11 providers pass Zod schema
- `npm test`: 14/14 tests pass
- `npm run build`: 15 static pages generated (11 provider routes)
- GitHub repo: https://github.com/nhouseholder/code-smart — 3 commits

## Bug Flags
- none — clean scan

## Verification
```bash
cd "/Volumes/Extreme Pro/ProjectsHQ/code-smart"
npm run validate   # → 11 providers ✓
npm test           # → 14 tests ✓
```

---

## Council Review Notes
Gemini 3-role review (REVISE verdict) caught 3 CI gaps before implementation:
1. `fetch-dispatch.yml` removed — AI vendor WAFs block GH Actions runner IPs
2. `permissions: issues: write` added to `daily-check.yml`
3. Duplicate JSON report format dropped (markdown only)

---

## Next Session Roadmap
1. **Deploy to Cloudflare Pages** — `wrangler.jsonc` exists; run opennextjs-cloudflare build then split `wrangler versions upload` / `wrangler versions deploy <uuid>@100%` (see `~/.kimi/WRANGLER_NOTES.md` for EPIPE workaround)
2. **Create `data-quality` GitHub label** — `gh label create "data-quality" --color "fbca04" --repo nhouseholder/code-smart` — required before first cron fires or `daily-check.yml` will error on issue creation
3. **Run first Playwright fetch pass** — `npx playwright install chromium` then `npm run fetch -- --provider anthropic --provider openai` — capture baseline screenshots, confirm report format is usable
4. **Expand test coverage** — `tests/data-loader.test.ts` needs `getAllProviders().length >= 11` assertion and per-new-provider coverage
5. **Fill benchmark data** — Qwen2.5-Coder and Kimi k1.5 have public HumanEval/SWE-bench scores; add to JSON to improve value-scorer accuracy
