# Handoff — code-smart — 2026-06-14

**Version:** v1.0.4
**HEAD:** `e97f4a6`
**Production:** (no live URL — local development only)

---

## Session Outcomes

| Item | Status |
|------|--------|
| Model name extraction from scraped text | ✅ Complete |
| Footnotes extraction (small-print caveats) | ✅ Complete |
| Assumptions recording (extraction metadata) | ✅ Complete |
| plan_model_access records written by pipeline | ✅ Complete |
| Seed expansion for docs/model source pages (5 providers) | ✅ Complete |
| "Content changed, no price changed" integration test | ✅ Complete |

---

## What Shipped
- `src/lib/scraper/model-extractor.ts` — Extracts AI model mentions from scraped text by known name (longest-match-first with overlap prevention, word-bounded regex, 120-char context snippet, dedup by position)
- `src/lib/scraper/annotation-scanner.ts` — `scanFootnotes()` (asterisk disclaimers, caveat keywords, "as of" markers, "terms apply" boilerplate) + `recordAssumptions()` (generates factual extraction metadata: price/limit/model counts, free tier detection, USD assumption)
- `src/db/migrations/0002_add_snapshot_notes.sql` — Added `notes TEXT` column to `source_snapshots` for JSON `{footnotes, assumptions}`
- `src/lib/scraper/pipeline.ts` — Full integration: knownModels loaded from DB, model extraction (pricing/docs pages), footnote scanning, assumption recording, notes JSON in snapshot insert, planModelAccess candidate writes (planId="" sentinel), modelMentions counter
- `src/db/schema.ts` — Added `notes` field to `sourceSnapshots` table definition
- `src/db/helpers.ts` — Added `notes` to `insertSourceSnapshot` data interface
- `src/db/seed.ts` — Added `docs_url` to ProviderJSON interface + conditional `pageType: "docs"` source page insertion
- 5 provider JSONs (`anthropic.json`, `openai.json`, `google.json`, `github-copilot.json`, `cursor.json`) — Added `docs_url` field pointing to model documentation pages
- `tests/scraper/model-extractor.test.ts` — 7 tests: single match, multiple, empty, long-name-preference, context snippet, dedup, field shape
- `tests/scraper/annotation-scanner.test.ts` — 10 tests: 6 footnote patterns + 4 assumption scenarios
- `tests/scraper/pipeline.test.ts` — Cosmetic change test (Gap 6) with planModelAccess assertions

## Bug Flags
None — clean scan. 3 test assertions fixed (overlap prevention logic, dedup expectation, footnote capitalization)

## Verification
```bash
pnpm test
```
114/114 tests passing (96 existing + 18 new).

---

## Next Session Roadmap
1. **Address pre-existing FK sentinel issue**: Pipeline writes `planId: ""` to planSnapshots/usageLimits/planModelAccess — sentinel plan only seeded in test fixtures, not in real seed. Running pipeline against real providers would hit FK constraint
2. **Update architecture docs**: `docs/architecture.md` and `docs/data-model.md` still reference the old architecture — Session 3 added 6 new extraction modules
3. **Extract shared test utilities**: `createTestDb()` / `runMigrations()` duplicated across 3 test files
4. **Scripts/scrape-providers.ts CLI polish**: Script created in Session 2 but untested — verify it works end-to-end with real provider pages
