# Handoff — 2026-06-17 · v1.2.0 · Senior review + ship

## Shipped
- **Version:** v1.2.0 (was v1.1.1)
- **HEAD SHA:** e14deaf (origin/main)
- **Live:** https://code-smart.pages.dev (CF Pages, branch=main) · this deploy: https://692665b5.code-smart.pages.dev

## What changed (Session 11 — senior-engineer review + polish)
- **Auto-stale confidence** — `effectiveConfidence()` / `weakenForStaleness()` (`src/lib/utils.ts`) downgrade any value >90d old to ≤`stale` at every scoring/badge boundary (value-scorer, rankings, ProvenanceBadge, plans/[id], freshness). Never upgrades.
- **Scraper robots.txt compliance** — new `src/lib/scraper/robots.ts` (RFC 9309 subset: UA-group most-specific-wins, Allow/Disallow longest-match, Crawl-Delay). `pipeline.ts` fetches+caches robots per host, skips (not fails) disallowed pages, honors `max(3s, Crawl-Delay)`.
- **Pipeline resilience** — `pipeline-daily.ts` `spawnWithRetry` (2 attempts, linear backoff), excludes read-only assertions. Idempotent (observedAt-keyed upserts).
- **/compare polish** — URL-backed filter+selection state (`?q/provider/band/conf/sel/diff`, deep-linkable, `<Suspense>`-wrapped for static export) + "Differences only" diff-highlight mode (`DIFF_ACCESSORS`).
- **Mobile tables** — reduced forced min-widths (Comparison 700→640, PlanComparison 640→480, ModelRanking 520→440) + sticky-left columns.
- **Cleanup/docs** — `windows.ts` dead locals removed, comments corrected to actual wall-clock scaling; CLAUDE.md deploy line fixed (static Pages, not Workers); CURRENT_STATE de-drifted; e2e harness aligned to `@playwright/test` 1.61; `test-results/` + `playwright-report/` gitignored.

## Preflight / verification (all PASS)
- typecheck: clean · lint: No issues
- unit tests: **290 passed** (23 files)
- quality-check: warnings only, exit 0 (plan-no-usage-estimate ×9, model-no-aa-mapping ×10, confidence-below-threshold ×31)
- build: static export OK → `out/`
- secret scan: clean (no keys in bundle)
- e2e: **12 passed** (clean single run; earlier red runs were port-5173 contention, not regressions)
- Review doc: `docs/senior-review.md`

## Live verification
- root 200 · /compare 200 · static API JSON valid
- New `/compare` Suspense fallback present in production HTML via cache-buster (edge cache lag on plain URL only)
- methodology.json version 3.1 (QAMU formula version — there is no app-version endpoint; package.json is build-only metadata)

## Launch readiness: 8/10
Gating a 9–10 (both bounded, tracked, non-blocking):
1. **AA benchmark freshness blind spot** — `aaConf` has no `accessed_date`, can't auto-stale.
2. **e2e breadth** — only 375px + URL state covered; want 768/1024 viewports + sort/interaction assertions in the deploy gate.

## Top 3 next priorities
1. Give AA benchmark scores an `accessed_date` so `aaConf` participates in auto-staleness (review rec #1).
2. Expand e2e viewport matrix (768/1024) + `ModelRankingTable` sortable-column test (review rec #4).
3. Replace empty-string FK sentinel in `pipeline.ts` (`planId: ""`) with a dedicated `__candidate__` row (review rec #2).
