# Handoff — code-smart — 2026-06-17

**Version:** v1.8.1
**HEAD:** `2ccfd2f`
**Production:** https://code-smart.pages.dev · confirmed 2026-06-17

---

## Session Outcomes

| Item | Status |
|------|--------|
| OpenAI Plus 160 msg/3h → 6M tokens/month | ✅ Shipped |
| Anthropic Pro 45 msg/5h → 90K tokens/month | ✅ Shipped |
| Anthropic Max 225 msg/5h → 450K tokens/month | ✅ Shipped |
| Cursor Pro $20/mo USD credits wired | ✅ Shipped |
| `"3h"` ResetWindow + active-hours extrapolation | ✅ Shipped |
| `messages_per_period` limit type + `reset_window` field | ✅ Shipped |
| Tiered stale-check (assumed=30d, observed/inferred=90d) | ✅ Shipped |
| 323 tests pass, typecheck clean | ✅ Verified |
| Deployed to Cloudflare Pages | ✅ Live |

---

## What Shipped

- `src/lib/schema.ts` — `messages_per_period` limit type; `reset_window` optional field on `UsageLimitSchema`
- `src/db/seed.ts` — reads `reset_window` from JSON (was hardcoded `null`); inline `PlanJSON` type updated
- `src/lib/normalization/types.ts` — `"3h"` added to `ResetWindow`
- `src/lib/normalization/windows.ts` — `"3h": 3` added to `WINDOW_HOURS`
- `src/lib/normalization/engine.ts` — `"3h"` extrapolation (active-hours: 20d × 5h/day / 3 = 33.3 windows/mo); Layer 7 valid windows updated
- `src/lib/normalization/config.ts` — `CURSOR_USD_RATE` mapping for cursor-pro; `NORMALIZATION_METHODOLOGY_VERSION` → `"1.2.0"`
- `src/data/providers/openai.json` — 160 msg/3h + 3000 msg/1w (Thinking) for openai-plus (observed)
- `src/data/providers/anthropic.json` — 45 msg/5h (pro) and 225 msg/5h (max) community estimates (assumed)
- `src/data/providers/cursor.json` — $20/mo `usd_credits_per_month` replacing stale requests_per_month:500
- `scripts/stale-check.ts` — tiered freshness thresholds
- `src/app/methodology/page.tsx` — Layer 7 includes "3h" + active-hours note
- `tests/data-loader.test.ts` — plan count 11→12 (stale fixture fix)

## Bug Flags

None — clean scan.

## Verification

```bash
cd "/Volumes/Extreme Pro/ProjectsHQ/code-smart"
pnpm test              # 323 passing
# Live site:
grep -o '6M\|90K\|450K' out/plans/openai-plus/index.html
grep -o '90K' out/plans/anthropic-pro/index.html
```

---

## Next Session Roadmap

1. **Add models to `src/data/providers/cursor.json`** — cursor-pro has `usage_limits` with $20/mo but zero models, so `generate:value-estimates` produces 0 estimates and `/plans/cursor-pro` shows "No value estimates". List cursor-pro's supported models (Claude Sonnet, GPT-5, Gemini Flash, etc.) to unlock the USD credits → token estimate pipeline.
2. **Wire `generate:value-estimates` into `pnpm build`** — currently a manual step; add it to the `build` script before `next build` in `package.json` so estimates are never stale in static output. Command: `"build": "pnpm generate:value-estimates && pnpm generate:rankings && pnpm generate:static-api && next build"`.
3. **Anthropic `assumed` limit re-verification (due 2026-07-17)** — community 45/225 msg/5h estimates hit the 30-day stale threshold in 30 days. Re-check `support.claude.com/en/articles/11647753` and update `accessed_date` + promote to `observed` if confirmed.
4. **Google Gemini Advanced** — still shows "—"; no absolute usage anchor. Monitor `support.google.com` for any published Gemini Advanced quota; the engineering is already in place to add a `messages_per_period` entry once a number exists.
5. **Add `anthropic-ratelimit-unified-*` header logging** — research found Anthropic sends undocumented headers tracking 5h, 7d, and 7d_sonnet windows. A lightweight proxy script capturing these could replace community-estimate with observed data programmatically.
